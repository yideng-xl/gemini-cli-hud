# Gemini CLI HUD 实施计划 (2026-03-23)

> **YOLO 模式执行建议：** 按照此计划快速迭代，保持 TDD (测试驱动开发) 以确保质量。

**目标：** 开发并交付 `gemini-cli-hud` 开源项目。

---

### 任务 1：独立扩展脚手架 (独立性保障)
- [ ] 初始化 `pnpm` 项目，配置 `tsconfig.json`。
- [ ] 安装核心依赖：`ink`, `react`, `typescript`, `vitest`。
- [ ] 创建 `SKILL.md` 确保扩展可被自动识别。

### 任务 2：实时数据拦截器 (核心逻辑)
- [ ] 编写拦截器测试，模拟 CLI 事件。
- [ ] 实现事件挂钩，捕获 Token 和工具数据。
- [ ] 数据聚合逻辑实现。

### 任务 3：终端 UI 开发 (视觉复刻)
- [ ] 实现 `ProgressBar` 通用组件。
- [ ] 构建 `HUD` 容器组件，复刻 `claude-hud` 布局。
- [ ] 处理终端底部的“粘性”渲染逻辑。

### 任务 4：开源与分发 (自动化安装)
- [ ] 编写 `src/index.ts` 自动初始化入口。
- [ ] 编写 `install.sh` 一键克隆/安装脚本。
- [ ] 编写完善的 `README.md`。
